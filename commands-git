Bonjour,

cloner un repo = git clone [url repo] / git init [nom repo] 

les commande push (dans l'ordre) :
 1) git add -all   
 2) git commit -m [commentaire du commit]
 3) git push <remote> <branch>    
 Par défaut si l’on veut push sur la branche principale, il suffit d’écrire :
 git push origin main
 
 Créer une branche de travail et switch dessus : git checkout -b [nom de la branche]
Passer sur la branche Main : git checkout main

Pour pull = git pull

git branch <branche> : permet de créer une nouvelle branche au repo en cours.

git checkout : permet de changer de branche 


